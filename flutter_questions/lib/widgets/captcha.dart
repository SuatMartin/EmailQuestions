/*import 'package:flutter/material.dart';
import 'package:flutter_recaptcha_v2_compat/flutter_recaptcha_v2_compat.dart';

class CaptchaWidget extends StatelessWidget {
  final Function(String) onVerified;

  const CaptchaWidget({Key? key, required this.onVerified}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ReCaptchaV2(
      apiKey: 'YOUR_GOOGLE_RECAPTCHA_SITE_KEY', // Use your own site key
      apiSecret: 'YOUR_GOOGLE_RECAPTCHA_SECRET_KEY', // Use your secret key for server-side verification
      onVerified: onVerified,
    );
  }
}*/